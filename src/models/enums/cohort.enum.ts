export enum CohortRuleDataTypes {
	DropDown = 'dropDown',
	Date = 'date',
}
export enum RuleTypes {
	Cohort = 'cohort',
	JoiningDate = 'joiningDate',
	CustomVariable = 'customVariable',
}
export enum RuleOperators {
	EQUAL = 'EQUAL',
	BETWEEN = 'BETWEEN',
	LESS_THAN = 'LESS_THAN',
	GREATER_THAN = 'GREATER_THAN',
	NOTEQUAL = 'NOTEQUAL',
}
